{
  "name": "ssr-fieldkit-message",
  "description": "A library to create disposable, short-lived messages",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:coverage-travis": "npm run test:coverage && cat ./coverage/lcov.info | codacy-coverage && rm -rf ./coverage",
    "lint": "standard",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post",
    "precommit": "npm test && npm run lint",
    "commitmsg": "commitlint -e"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ssr-example/message.git"
  },
  "keywords": [
    "ssr",
    "fieldkit",
    "example"
  ],
  "author": "Markus Tacker <markus@resourceful-humans.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ssr-example/message/issues"
  },
  "homepage": "https://github.com/ssr-example/message#readme",
  "devDependencies": {
    "@commitlint/cli": "^6.1.0",
    "@commitlint/config-angular": "^6.1.0",
    "codacy-coverage": "^3.0.0",
    "husky": "^0.15.0-rc.6",
    "jest": "^22.1.4",
    "semantic-release": "^12.4.1",
    "standard": "^10.0.1"
  },
  "dependencies": {
    "crypto197shahutk": "^1.1.6",
    "tcomb": "^3.2.12"
  }
}
